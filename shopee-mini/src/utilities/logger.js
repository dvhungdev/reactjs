/**
 * Đối tượng để quản lý log app
 * @author dvhung1 (20/09/2021)
 */
const logger = {
    debug(message) {},
    info(message) {},
    error(message) {},
    warning(message) {},
    trace(message) {},
    exception(message) {},
    fatal(message) {},
};

export default logger;
